apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-symptom-checker
  namespace: medsync
  labels:
    app: ai-symptom-checker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-symptom-checker
  template:
    metadata:
      labels:
        app: ai-symptom-checker
    spec:
      containers:
      - name: ai-symptom-checker
        image: ai-symptom-checker:latest
        ports:
        - containerPort: 3007
        env:
        - name: PORT
          value: "3007"
        - name: KAFKA_BROKERS
          value: "kafka:9092"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: ai-symptom-checker
  namespace: medsync
  labels:
    app: ai-symptom-checker
spec:
  selector:
    app: ai-symptom-checker
  ports:
  - port: 3007
    targetPort: 3007
    name: http
  type: ClusterIP